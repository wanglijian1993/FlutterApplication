import 'package:flutter/cupertino.dart';

class OfficialAccountPage extends StatelessWidget {
  const OfficialAccountPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("OfficialAccount"),
    );
  }
}
